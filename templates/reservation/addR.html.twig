{% extends 'back.html.twig' %}
{% block body %}
        {{form_start(fR)}}
        {{form_label(fR.numero,'Nombre de personne')}}
        {{form_widget(fR.numero)}}
        {{form_end(fR)}}
{% endblock %}